# Evidence

`campaign-summary.json` is the public index. `runs/` contains the exact safe subset of the current live run. `terminal-bench/` and `deep-swe/` contain selected harness evidence and derived summaries.

Files omitted from the private source are named in the repository security and limitation documents. No omitted file is silently represented as published.
