{
  "artifacts": {
    "blind_spots": [
      "No seat is given the actual parser implementation, so all plans assume a pure function over whole-transcript strings and cannot verify real code paths, streaming APIs, or error object shapes.",
      "Exact on-wire grammar of “actual nonzero exit marker” remains underspecified beyond the single `[exit 0]` example; correlated assumption risk if the real plugin uses a different trailer dialect.",
      "Dependency-freedom cannot be evidenced from the supplied transcript alone; any strong “zero deps proven” claim exceeds mechanical evidence.",
      "Limited treatment of encoding (CRLF vs LF), binary/NUL payloads, and extremely large inputs as systematic reject/accept policies.",
      "No seat fully resolves whether command-prefix/`$` validation is in scope versus exit-marker scanning only."
    ],
    "budget_remaining": {
      "calls": {
        "consumed": 10,
        "limit": 40,
        "remaining": 30
      },
      "input_tokens": {
        "consumed": 54021,
        "limit": 1200000,
        "remaining": 1145979
      },
      "known_cost_usd": {
        "consumed": 0.2681,
        "limit": 100.0,
        "remaining": 99.7319
      },
      "output_tokens": {
        "consumed": 27039,
        "limit": 400000,
        "remaining": 372961
      },
      "provider_cost_usd": {
        "openrouter": {
          "consumed": 0.0,
          "limit": 50.0,
          "remaining": 50.0
        },
        "openrouter_native_fusion": {
          "consumed": 0.0,
          "limit": 50.0,
          "remaining": 50.0
        },
        "trusted_router": {
          "consumed": 0.0,
          "limit": 50.0,
          "remaining": 50.0
        },
        "xai_direct": {
          "consumed": 0.2681,
          "limit": 75.0,
          "remaining": 74.7319
        }
      },
      "reasoning_tokens": {
        "consumed": 9297,
        "limit": 400000,
        "remaining": 390703
      },
      "tool_calls": {
        "consumed": 0,
        "limit": 64,
        "remaining": 64
      },
      "total_tokens": {
        "consumed": 81060,
        "limit": 2000000,
        "remaining": 1918940
      },
      "unknown_cost_calls": 0,
      "wall_seconds": {
        "consumed": 343.554506542,
        "limit": 7200,
        "remaining": 6856.445493458
      },
      "warnings": []
    },
    "constraints": {
      "max_fix_cycles": 2,
      "preserve_unrelated_changes": true,
      "remote_models_may_write_workspace": false,
      "require_user_approval_for_destructive_actions": true,
      "require_user_approval_for_external_writes": true,
      "sandbox_mode": "workspace_write",
      "stop_on_test_failure": false,
      "workspace_scope": "user_requested_only"
    },
    "fused_plan": "# Deterministic verification plan: evidence-parser exit markers\n\n## Status and scope\n\n**Plan type:** Pre-execution, host-applied only.  \n**Seat/workspace claims:** None. No files were inspected, no commands were run, and no plugin code was executed in producing this plan. Current workspace state is unknown.  \n**Sole supplied mechanical evidence (baseline only):**\n\n```text\n$ regression-test\none parser regression test passed\n[exit 0]\n```\n\nThat block supports only “happy-path accept for this exact transcript.” It does **not** prove nonzero rejection, echo-literal ignore, dependency-freedom, or boundary robustness. Those behaviors are verified only after the host applies the frozen vectors below.\n\n**Profile note (context):** When the host later runs an accepted plan, do so under the frontier-only default profile / release-smoke scope. Profile machinery is not a seat-side gate and is not evidenced here.\n\n---\n\n## Locked parser rules (normative for this plan)\n\nThese rules make every vector single-valued. The host compares parser output to the expected outcome exactly.\n\n| ID | Rule |\n|----|------|\n| R1 | **Marker form:** A *structural exit marker* is a whole line consisting of exactly `[exit N]` where `N` is an unsigned decimal integer with no sign, no leading zeros (except `N=0` alone), no surrounding whitespace inside the brackets, and LF line discipline after normalization (CRLF → LF). Examples that **are** markers: `[exit 0]`, `[exit 1]`, `[exit 42]`. |\n| R2 | **Accept vs reject:** If the transcript’s *governing* structural marker has `N=0` → **Accept**. If it has `N≠0` → **Reject**. |\n| R3 | **Governing marker:** Among structural markers on their own lines, the **last** one in the transcript governs. Mid-line text such as `echo [exit 1]` is **not** a structural marker. |\n| R4 | **Literal ignore:** Substrings `exit 1`, `exit 0`, or even the characters `[exit 1]` appearing inside command lines, quotes, or non-marker output do **not** set status. Only R1 structural markers do. |\n| R5 | **Non-markers (ignore for status):** Bare `exit 1`, `(exit 1)`, `[EXIT 1]`, `[exit 01]`, `[exit -1]`, `[exit 1 ]`, `[exit 1`, `exit\\t1`, fullwidth digits, or marker text not alone on a line. These never create Accept/Reject by themselves. |\n| R6 | **Absent marker:** If zero structural markers exist after applying R1–R5 → outcome **Malformed** (not Accept, not Reject-as-nonzero). |\n| R7 | **Malformed boundaries:** Truncated markers, unbalanced brackets on a candidate line, embedded NUL, or empty transcript → **Malformed**. Do **not** invent a nonzero exit code from garbage. Conflicting well-formed structural markers still follow R3 (last wins); that is Reject/Accept, not Malformed. |\n| R8 | **Dependency-free harness:** Verification is pure string in → `{Accept\\|Reject\\|Malformed}` out. No network, no third-party libs required for the plan harness. Proving the plugin’s own dependency inventory is **optional host hardening** and is **not** required for PLAN EXECUTION PASS (supplied evidence cannot support a dep inventory claim). |\n| R9 | **Precedence:** Classify structural markers (R1) first; apply R3; only then treat remaining text as payload (R4–R5). Mixed “literal exit 1 + structural nonzero” → **Reject**. |\n\n**Reject signaling:** Host maps parser failure/reject/nonzero to plan outcome **Reject**; success/ok/zero to **Accept**; explicit parse-error/boundary-fail to **Malformed**. If the implementation has no third channel, map its “error” path to **Malformed** for V6/V7 and document the mapping in the evidence pack.\n\n---\n\n## Outcome vocabulary (exact, single-valued)\n\nEvery vector expects exactly one of:\n\n- **Accept**\n- **Reject**\n- **Malformed**\n\n---\n\n## Frozen fixture table\n\nAll inputs are complete, copy-pasteable transcripts. Newlines are LF (`\\n`). Vectors are named and ordered; the host must not re-instantiate from patterns.\n\n### V0 — Supplied mechanical baseline (Accept)\n\n**Input (exact):**\n\n```text\n$ regression-test\none parser regression test passed\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n### V1 — Structural nonzero reject\n\n**V1a — `[exit 1]` trailer (Reject)**  \n**Input:**\n\n```text\n$ regression-test\none parser regression test passed\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V1b — `[exit 2]` trailer (Reject)**  \n**Input:**\n\n```text\n$ regression-test\none parser regression test passed\n[exit 2]\n```\n\n**Expected:** Reject  \n\n**V1c — `[exit 255]` trailer (Reject)**  \n**Input:**\n\n```text\n$ cmd\noutput\n[exit 255]\n```\n\n**Expected:** Reject  \n\n**V1d — minimal nonzero (Reject)**  \n**Input:**\n\n```text\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V1e — minimal zero (Accept)**  \n**Input:**\n\n```text\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n### V2 — Literal `exit 1` in echoed command; trailer governs\n\n**V2a — echo quoted literal + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ echo \"exit 1\"\nexit 1\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V2b — echo quoted literal + nonzero trailer (Reject)**  \n**Input:**\n\n```text\n$ echo \"exit 1\"\nexit 1\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V2c — unquoted echo + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ echo exit 1\nexit 1\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V2d — false; echo + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ false; echo exit 1\nexit 1\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V2e — printf-style payload + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ printf '%s\\n' 'exit 1'\nexit 1\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V2f — long sentence containing exit 1 + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ echo \"the status was exit 1 in the message\"\nthe status was exit 1 in the message\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n### V3 — Structural precedence over payload\n\n**V3a — payload looks like success marker; true trailer nonzero (Reject)**  \n**Input:**\n\n```text\n$ run\n[exit 0]\nstill running\n[exit 1]\n```\n\n**Expected:** Reject  \n(Last structural marker is `[exit 1]`.)\n\n**V3b — payload contains `[exit 1]` mid-line; trailer zero (Accept)**  \n**Input:**\n\n```text\n$ echo [exit 1]\n[exit 1]\n[exit 0]\n```\n\n**Expected:** Accept  \n(Line `[exit 1]` alone would be structural if it were last; here last line is `[exit 0]`. Note: the middle line *is* a structural marker under R1, but R3 last-wins → Accept.)\n\n**V3c — mixed echo literal + final nonzero (Reject)**  \n**Input:**\n\n```text\n$ echo \"run exit 1 now\"\nrun exit 1 now\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V3d — two structural markers, last zero (Accept)**  \n**Input:**\n\n```text\n$ step\n[exit 1]\nretry ok\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n### V4 — Non-marker forms (must not drive nonzero Reject)\n\n**V4a — bare `exit 1` only (Malformed)**  \n**Input:**\n\n```text\n$ echo hi\nhi\nexit 1\n```\n\n**Expected:** Malformed  \n(No R1 structural marker.)\n\n**V4b — parenthesized form + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ (exit 1)\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V4c — uppercase variant only (Malformed)**  \n**Input:**\n\n```text\n$ cmd\n[EXIT 1]\n```\n\n**Expected:** Malformed  \n\n**V4d — leading-zero variant only (Malformed)**  \n**Input:**\n\n```text\n$ cmd\n[exit 01]\n```\n\n**Expected:** Malformed  \n\n**V4e — internal whitespace variant only (Malformed)**  \n**Input:**\n\n```text\n$ cmd\n[exit 1 ]\n```\n\n**Expected:** Malformed  \n\n**V4f — mid-line marker-like text + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ echo foo [exit 1] bar\nfoo [exit 1] bar\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V4g — trailing semicolon junk line + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ cmd\nexit 1;\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n### V5 — Empty / trailer-only / whitespace body\n\n**V5a — empty string (Malformed)**  \n**Input:** *(empty — zero bytes)*  \n\n**Expected:** Malformed  \n\n**V5b — whitespace only (Malformed)**  \n**Input:**\n\n```text\n\n\n```\n\n**Expected:** Malformed  \n(Exact bytes: `\\n\\n` only.)\n\n**V5c — body + zero (Accept)**  \n**Input:**\n\n```text\nsome output\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V5d — body + nonzero (Reject)**  \n**Input:**\n\n```text\nsome output\n[exit 1]\n```\n\n**Expected:** Reject  \n\n---\n\n### V6 — Absent vs truncated (Malformed; do not invent nonzero)\n\n**V6a — no marker after output (Malformed)**  \n**Input:**\n\n```text\n$ regression-test\none parser regression test passed\n```\n\n**Expected:** Malformed  \n\n**V6b — truncated opener, no close (Malformed)**  \n**Input:**\n\n```text\n$ cmd\noutput\n[exit 1\n```\n\n**Expected:** Malformed  \n\n**V6c — truncated empty brackets line (Malformed)**  \n**Input:**\n\n```text\n$ cmd\n[exit\n```\n\n**Expected:** Malformed  \n\n**V6d — close bracket missing digit (Malformed)**  \n**Input:**\n\n```text\n$ cmd\n[exit ]\n```\n\n**Expected:** Malformed  \n\n---\n\n### V7 — Boundary and adversarial fixtures (exact bytes)\n\n**V7a — duplicate final markers last nonzero (Reject)**  \n**Input:**\n\n```text\n$ cmd\nok\n[exit 0]\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V7b — duplicate final markers last zero (Accept)**  \n**Input:**\n\n```text\n$ cmd\nbad\n[exit 1]\n[exit 0]\n```\n\n**Expected:** Accept  \n\n**V7c — missing final newline after zero marker (Accept)**  \n**Input (exact; final line has no trailing `\\n`):**  \nbytes = `$ cmd\\ndone\\n[exit 0]`  \n\nFor copy-paste clarity, the three lines are `$ cmd`, `done`, `[exit 0` with **no** newline after the closing `]` of the marker. Host must feed exactly that byte string.\n\n**Expected:** Accept  \n(R1 whole-line marker after LF normalization of prior lines; absence of trailing newline after the marker line does not strip marker status.)\n\n**V7d — CRLF line endings with zero marker (Accept)**  \n**Input (exact CRLF):**  \nbytes = `$ regression-test\\r\\none parser regression test passed\\r\\n[exit 0]\\r\\n`  \n\n**Expected:** Accept  \n(Host normalizes CRLF→LF before R1, or parser accepts CRLF equivalently; either way outcome must be Accept.)\n\n**V7e — CRLF with nonzero marker (Reject)**  \n**Input (exact CRLF):**  \nbytes = `$ regression-test\\r\\none parser regression test passed\\r\\n[exit 1]\\r\\n`  \n\n**Expected:** Reject  \n\n**V7f — embedded NUL before zero trailer (Malformed)**  \n**Input (exact):**  \nbytes = `$ cmd\\nout\\x00put\\n[exit 0]\\n`  \n\n**Expected:** Malformed  \n(NUL ⇒ boundary/encoding failure per R7; do not Accept merely because a zero trailer appears after binary corruption.)\n\n**V7g — nested transcript-looking echo + zero trailer (Accept)**  \n**Input:**\n\n```text\n$ cat <<'EOF'\n$ inner\n[exit 1]\nEOF\n$ inner\n[exit 1]\n[exit 0]\n```\n\n**Expected:** Accept  \n(Last structural marker is `[exit 0]`. Payload may contain full fake transcripts.)\n\n**V7h — nested fake transcript + nonzero trailer (Reject)**  \n**Input:**\n\n```text\n$ cat <<'EOF'\n$ inner\n[exit 0]\nEOF\n$ inner\n[exit 0]\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V7i — only prompt line (Malformed)**  \n**Input:**\n\n```text\n$ \n```\n\n**Expected:** Malformed  \n\n**V7j — marker immediately after prompt line (Accept)**  \n**Input:**\n\n```text\n$ true\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n### V8 — Mutation oracle (derived from V0; exact strings)\n\n**V8a — V0 with final marker flipped to 1 (Reject)**  \n**Input:**\n\n```text\n$ regression-test\none parser regression test passed\n[exit 1]\n```\n\n**Expected:** Reject  \n\n**V8b — V0 with literal inserted early; marker still 0 (Accept)**  \n**Input:**\n\n```text\n$ regression-test\nnote: would have been exit 1\none parser regression test passed\n[exit 0]\n```\n\n**Expected:** Accept  \n\n---\n\n## Host execution procedure (pending)\n\nAll steps are **pending** until the active host runs them. No outcomes below are claimed as observed.\n\n1. **Normalize harness I/O**  \n   - Implement or wrap `parse_evidence(transcript: str | bytes) -> Accept | Reject | Malformed` per R1–R9.  \n   - Record how the real plugin’s API maps onto the three outcomes.\n\n2. **Fixed order run**  \n   Run vectors in this order only:  \n   `V0, V1a, V1b, V1c, V1d, V1e, V2a, V2b, V2c, V2d, V2e, V2f, V3a, V3b, V3c, V3d, V4a, V4b, V4c, V4d, V4e, V4f, V4g, V5a, V5b, V5c, V5d, V6a, V6b, V6c, V6d, V7a, V7b, V7c, V7d, V7e, V7f, V7g, V7h, V7i, V7j, V8a, V8b`.\n\n3. **Per-vector compare**  \n   For each ID: feed the **exact** input bytes/string; compare actual outcome to the single expected outcome.  \n   On first mismatch: **abort**, record `FAIL <vector_id> expected=... actual=...`.\n\n4. **Idempotence (pending)**  \n   Re-parse V0, V2a, V1a, V6b once each; second result must equal first.\n\n5. **Optional host hardening (not in aggregate pass)**  \n   - Static dependency inventory of the plugin.  \n   - Frontier-only default profile smoke log wrapping the same fixture table.  \n   - Fixture hashes in the evidence pack.  \n   These must not replace or relax the fixture table.\n\n6. **Evidence pack (host returns)**  \n   - Predicate table: `vector_id → expected → actual → pass/fail`.  \n   - Full transcripts for any failure.  \n   - Explicit residual paragraph on malformed boundaries (mandatory; see below).  \n   - Statement that V0 supplied evidence alone did not prove V1–V8.\n\n---\n\n## Aggregate gate\n\n**PLAN EXECUTION PASS** if and only if:\n\n1. Every vector in the frozen table (V0–V8b inclusive) yields its **single** normative expected outcome; and  \n2. Idempotence checks on V0, V2a, V1a, V6b pass; and  \n3. The evidence pack includes the mandatory malformed-boundary residual paragraph below.\n\n**PLAN EXECUTION FAIL** if any listed vector mismatches, any vector was substituted with a different fixture under the same ID, or the residual paragraph is omitted.\n\nOptional dep inventory / profile smoke may be reported separately; they are **not** required for PLAN EXECUTION PASS under this plan’s evidence constraints.\n\n---\n\n## Minority residual (mandatory — not discharged by green happy-path)\n\n**Malformed transcript boundaries** remain a non-blocking residual risk even if every vector above passes:\n\n- Truncated or split markers, missing delimiters, fused multi-session transcripts, dual conflicting trailers under alternate grammars, non-UTF8/control bytes, and streaming chunk boundaries can still cause false Accept or false Reject in production paths not identical to these whole-string fixtures.\n- Policy locked here: **do not invent nonzero exit status from garbage** (truncated `[exit 1` → Malformed, not Reject). Fail closed into **Malformed** for absent/truncated/NUL cases rather than silently repairing boundaries.\n- Green results on V0–V8b do **not** prove streaming correctness, partial-read assembly, or all encoding shift cases.\n- Host release notes / evidence pack **must** restate this residual and point at V6a–d and V7c–f as the dedicated negative/boundary slice.\n\nSupported emphases retained:\n\n- Prefer not false-rejecting by synthesizing nonzero codes from partial markers.  \n- Never silently repair broken boundaries.  \n- Future hardening (single canonical trailer window, reject on undecidable fusion, length/checksum sidecar) is out of scope for this pass gate but recommended next iteration.\n\n---\n\n## Assumptions and remaining uncertainty (explicit)\n\n| Topic | Plan choice | Residual uncertainty |\n|-------|-------------|----------------------|\n| Marker grammar | Exact whole-line `[exit N]` per R1 | Real plugin may use another dialect (`exit code: N`, bare status). If so, host must remap dialect → R1 equivalent **or** fail plan with grammar mismatch—do not silently broaden fixtures. |\n| Bare `exit 1` | Non-marker → Malformed if alone | Plugin might treat bare status as structural; that would fail V4a against this plan and requires a documented rule change + fixture revision. |\n| Multiple markers | Last wins (R3) | Alternate “any nonzero rejects” policy would fail V3d/V7b; lock is intentional. |\n| Absent marker | Malformed | Not success-by-default. |\n| Reject API shape | Exception vs structured object | Host documents mapping; plan only requires three-way classification. |\n| Dependency-free plugin | Harness is dep-free; plugin inventory optional | Cannot be proven from supplied `[exit 0]` transcript alone. |\n| Command-prefix `$` validation | Out of scope; markers only | Untested whether invalid prompts matter. |\n| Unicode homoglyphs | Not all homoglyphs enumerated | V4 non-ASCII beyond NUL/CRLF not exhaustively listed; residual risk. |\n\n---\n\n## Failure handling\n\n| Failure | Host action |\n|---------|-------------|\n| Any vector mismatch | FAIL plan; do not ship parser as verified for this contract |\n| Parser lacks Malformed channel | Map errors → Malformed; if absent markers become Accept, FAIL V6a/V4a/V5a |\n| Grammar differs from R1 | Record mismatch; either adapt documented dialect in a revised plan or reject implementation |\n| Nondeterministic results on re-parse | FAIL idempotence |\n| Host cannot feed exact bytes (NUL/CRLF) | FAIL V7d–f with environment note; do not skip quietly |\n\n---\n\n## Completeness checklist (pre-execution)\n\n- [x] Reject actual nonzero structural markers (V1*, V2b, V3a/c, V5d, V7a/e/h, V8a)  \n- [x] Ignore literal `exit 1` inside echoed commands when trailer is zero (V2a/c/d/e/f, V8b)  \n- [x] Structural precedence over payload (V3*)  \n- [x] Supplied evidence baseline Accept (V0) without over-claiming  \n- [x] Every vector has fully embedded exact input and exactly one expected outcome  \n- [x] Malformed-boundary minority risk preserved with dedicated fixtures + mandatory residual paragraph  \n- [x] No seat workspace access or execution claimed beyond supplied evidence  \n- [x] Aggregate pass defined solely on the frozen fixture table + idempotence + residual paragraph  \n\n**Pending:** All actual parse results, smoke logs, and pass/fail marks await active host execution.",
    "minority_findings": [
      "Malformed transcript boundaries are the mandated minority residual across all seats: truncated/partial markers, missing final newline, split/fused transcripts, dual conflicting trailers, and non-UTF8/control bytes can cause false accept or false reject; well-formed `[exit 0]` evidence does not resolve this.",
      "Seat A’s supported lone emphasis: do not false-reject by inventing a nonzero exit from garbage/truncated markers; document boundary behavior explicitly even if the core suite passes.",
      "Seat B’s supported lone emphasis: keep fail-closed as default safety posture, mandate a dedicated boundary negative test and release-note residual, and never silently repair broken boundaries.",
      "Seat C’s supported lone emphasis: future hardening options (single canonical marker in final N lines, reject conflicting trailers, length/checksum sidecar) without blocking the current plan."
    ],
    "required_checks": {
      "completion_requires": [
        "requested_change",
        "verification_evidence",
        "post_execution_pass"
      ],
      "lifecycle_gates": {
        "final": {
          "owner": "codex_host",
          "required_evidence": [
            "gate_verdict",
            "cost_ledger",
            "provenance"
          ],
          "timeout_seconds": 900,
          "tool_policy": "none"
        },
        "plan": {
          "owner": "codex_host",
          "required_evidence": [
            "requirements_trace",
            "risk_analysis"
          ],
          "timeout_seconds": 900,
          "tool_policy": "none"
        },
        "post_execution": {
          "owner": "codex_host",
          "required_evidence": [
            "diff",
            "tests",
            "requirement_coverage"
          ],
          "timeout_seconds": 1200,
          "tool_policy": "none"
        },
        "pre_execution": {
          "owner": "codex_host",
          "required_evidence": [
            "approved_fused_plan",
            "scope_boundaries"
          ],
          "timeout_seconds": 900,
          "tool_policy": "none"
        },
        "summarize": {
          "owner": "codex_host",
          "required_evidence": [
            "decisions",
            "open_risks",
            "verification_state"
          ],
          "timeout_seconds": 600,
          "tool_policy": "none"
        }
      },
      "require_diff_review": true,
      "run_tests": true
    }
  },
  "backend": "active_codex",
  "blocking_reasons": [],
  "execution_contract": {
    "allow_recursive_codex_cli": false,
    "codex_binary": "codex",
    "completion_requires": [
      "requested_change",
      "verification_evidence",
      "post_execution_pass"
    ],
    "enabled": true,
    "handoff_include": [
      "fused_plan",
      "constraints",
      "minority_findings",
      "blind_spots",
      "required_checks",
      "budget_remaining"
    ],
    "max_fix_cycles": 2,
    "mode": "codex_handoff",
    "model": "gpt-5.6-sol",
    "native_codex": {
      "enabled": true,
      "executor_model": "gpt-5.6-sol",
      "executor_reasoning_effort": "xhigh",
      "inherit_workspace_context": true,
      "max_parallel_agents": 3,
      "mode": "host_handoff",
      "reasoning_only_roles": [],
      "require_fusion_before_execution": true,
      "require_gate_after_execution": true,
      "reviewer_models": [
        "gpt-5.6-sol"
      ],
      "reviewer_reasoning_effort": "xhigh",
      "reviewer_roles": []
    },
    "preserve_unrelated_changes": true,
    "reasoning_effort": "xhigh",
    "remote_models_may_write_workspace": false,
    "require_diff_review": true,
    "require_fused_plan": true,
    "require_post_execution_gate": true,
    "require_pre_execution_gate": true,
    "require_user_approval_for_destructive_actions": true,
    "require_user_approval_for_external_writes": true,
    "run_tests": true,
    "sandbox_mode": "workspace_write",
    "stop_on_test_failure": false,
    "timeout_seconds": 3600,
    "workspace_scope": "user_requested_only"
  },
  "execution_contract_sha256": "3308e32418889aeba0e9f4b9526c20db965007b868cf6f1a7f86273ad7adbe39",
  "handoff_contract_sha256": "ff0c989dd2039653ee4f95711511593b823e197baef435889fb1c851efbaf9bb",
  "handoff_payload_sha256": "fa7e246691d774d7110e73deb34b526f51e69222d316c333913a8b3bb26e04bb",
  "included_sections": [
    "fused_plan",
    "constraints",
    "minority_findings",
    "blind_spots",
    "required_checks",
    "budget_remaining"
  ],
  "instruction": "This is a persisted Codex host-workflow packet, not independent permission to act. Re-inspect the current workspace, preserve user changes, honor newer user instructions and the live sandbox/approval policy, and stop if repository reality contradicts the packet. Do not mutate files or external state yet. The active Codex host must independently invoke plan, pre_execution lifecycle gate(s) with their required evidence and retain their same-artifact receipts first. After implementation, the active Codex host must invoke the configured post_execution, final, summarize gate(s) over the exact resulting artifact and evidence.\n\nSelected handoff artifacts:\n{\n  \"blind_spots\": [\n    \"No seat is given the actual parser implementation, so all plans assume a pure function over whole-transcript strings and cannot verify real code paths, streaming APIs, or error object shapes.\",\n    \"Exact on-wire grammar of “actual nonzero exit marker” remains underspecified beyond the single `[exit 0]` example; correlated assumption risk if the real plugin uses a different trailer dialect.\",\n    \"Dependency-freedom cannot be evidenced from the supplied transcript alone; any strong “zero deps proven” claim exceeds mechanical evidence.\",\n    \"Limited treatment of encoding (CRLF vs LF), binary/NUL payloads, and extremely large inputs as systematic reject/accept policies.\",\n    \"No seat fully resolves whether command-prefix/`$` validation is in scope versus exit-marker scanning only.\"\n  ],\n  \"budget_remaining\": {\n    \"calls\": {\n      \"consumed\": 10,\n      \"limit\": 40,\n      \"remaining\": 30\n    },\n    \"input_tokens\": {\n      \"consumed\": 54021,\n      \"limit\": 1200000,\n      \"remaining\": 1145979\n    },\n    \"known_cost_usd\": {\n      \"consumed\": 0.2681,\n      \"limit\": 100.0,\n      \"remaining\": 99.7319\n    },\n    \"output_tokens\": {\n      \"consumed\": 27039,\n      \"limit\": 400000,\n      \"remaining\": 372961\n    },\n    \"provider_cost_usd\": {\n      \"openrouter\": {\n        \"consumed\": 0.0,\n        \"limit\": 50.0,\n        \"remaining\": 50.0\n      },\n      \"openrouter_native_fusion\": {\n        \"consumed\": 0.0,\n        \"limit\": 50.0,\n        \"remaining\": 50.0\n      },\n      \"trusted_router\": {\n        \"consumed\": 0.0,\n        \"limit\": 50.0,\n        \"remaining\": 50.0\n      },\n      \"xai_direct\": {\n        \"consumed\": 0.2681,\n        \"limit\": 75.0,\n        \"remaining\": 74.7319\n      }\n    },\n    \"reasoning_tokens\": {\n      \"consumed\": 9297,\n      \"limit\": 400000,\n      \"remaining\": 390703\n    },\n    \"tool_calls\": {\n      \"consumed\": 0,\n      \"limit\": 64,\n      \"remaining\": 64\n    },\n    \"total_tokens\": {\n      \"consumed\": 81060,\n      \"limit\": 2000000,\n      \"remaining\": 1918940\n    },\n    \"unknown_cost_calls\": 0,\n    \"wall_seconds\": {\n      \"consumed\": 343.554506542,\n      \"limit\": 7200,\n      \"remaining\": 6856.445493458\n    },\n    \"warnings\": []\n  },\n  \"constraints\": {\n    \"max_fix_cycles\": 2,\n    \"preserve_unrelated_changes\": true,\n    \"remote_models_may_write_workspace\": false,\n    \"require_user_approval_for_destructive_actions\": true,\n    \"require_user_approval_for_external_writes\": true,\n    \"sandbox_mode\": \"workspace_write\",\n    \"stop_on_test_failure\": false,\n    \"workspace_scope\": \"user_requested_only\"\n  },\n  \"fused_plan\": \"# Deterministic verification plan: evidence-parser exit markers\\n\\n## Status and scope\\n\\n**Plan type:** Pre-execution, host-applied only.  \\n**Seat/workspace claims:** None. No files were inspected, no commands were run, and no plugin code was executed in producing this plan. Current workspace state is unknown.  \\n**Sole supplied mechanical evidence (baseline only):**\\n\\n```text\\n$ regression-test\\none parser regression test passed\\n[exit 0]\\n```\\n\\nThat block supports only “happy-path accept for this exact transcript.” It does **not** prove nonzero rejection, echo-literal ignore, dependency-freedom, or boundary robustness. Those behaviors are verified only after the host applies the frozen vectors below.\\n\\n**Profile note (context):** When the host later runs an accepted plan, do so under the frontier-only default profile / release-smoke scope. Profile machinery is not a seat-side gate and is not evidenced here.\\n\\n---\\n\\n## Locked parser rules (normative for this plan)\\n\\nThese rules make every vector single-valued. The host compares parser output to the expected outcome exactly.\\n\\n| ID | Rule |\\n|----|------|\\n| R1 | **Marker form:** A *structural exit marker* is a whole line consisting of exactly `[exit N]` where `N` is an unsigned decimal integer with no sign, no leading zeros (except `N=0` alone), no surrounding whitespace inside the brackets, and LF line discipline after normalization (CRLF → LF). Examples that **are** markers: `[exit 0]`, `[exit 1]`, `[exit 42]`. |\\n| R2 | **Accept vs reject:** If the transcript’s *governing* structural marker has `N=0` → **Accept**. If it has `N≠0` → **Reject**. |\\n| R3 | **Governing marker:** Among structural markers on their own lines, the **last** one in the transcript governs. Mid-line text such as `echo [exit 1]` is **not** a structural marker. |\\n| R4 | **Literal ignore:** Substrings `exit 1`, `exit 0`, or even the characters `[exit 1]` appearing inside command lines, quotes, or non-marker output do **not** set status. Only R1 structural markers do. |\\n| R5 | **Non-markers (ignore for status):** Bare `exit 1`, `(exit 1)`, `[EXIT 1]`, `[exit 01]`, `[exit -1]`, `[exit 1 ]`, `[exit 1`, `exit\\\\t1`, fullwidth digits, or marker text not alone on a line. These never create Accept/Reject by themselves. |\\n| R6 | **Absent marker:** If zero structural markers exist after applying R1–R5 → outcome **Malformed** (not Accept, not Reject-as-nonzero). |\\n| R7 | **Malformed boundaries:** Truncated markers, unbalanced brackets on a candidate line, embedded NUL, or empty transcript → **Malformed**. Do **not** invent a nonzero exit code from garbage. Conflicting well-formed structural markers still follow R3 (last wins); that is Reject/Accept, not Malformed. |\\n| R8 | **Dependency-free harness:** Verification is pure string in → `{Accept\\\\|Reject\\\\|Malformed}` out. No network, no third-party libs required for the plan harness. Proving the plugin’s own dependency inventory is **optional host hardening** and is **not** required for PLAN EXECUTION PASS (supplied evidence cannot support a dep inventory claim). |\\n| R9 | **Precedence:** Classify structural markers (R1) first; apply R3; only then treat remaining text as payload (R4–R5). Mixed “literal exit 1 + structural nonzero” → **Reject**. |\\n\\n**Reject signaling:** Host maps parser failure/reject/nonzero to plan outcome **Reject**; success/ok/zero to **Accept**; explicit parse-error/boundary-fail to **Malformed**. If the implementation has no third channel, map its “error” path to **Malformed** for V6/V7 and document the mapping in the evidence pack.\\n\\n---\\n\\n## Outcome vocabulary (exact, single-valued)\\n\\nEvery vector expects exactly one of:\\n\\n- **Accept**\\n- **Reject**\\n- **Malformed**\\n\\n---\\n\\n## Frozen fixture table\\n\\nAll inputs are complete, copy-pasteable transcripts. Newlines are LF (`\\\\n`). Vectors are named and ordered; the host must not re-instantiate from patterns.\\n\\n### V0 — Supplied mechanical baseline (Accept)\\n\\n**Input (exact):**\\n\\n```text\\n$ regression-test\\none parser regression test passed\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n### V1 — Structural nonzero reject\\n\\n**V1a — `[exit 1]` trailer (Reject)**  \\n**Input:**\\n\\n```text\\n$ regression-test\\none parser regression test passed\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V1b — `[exit 2]` trailer (Reject)**  \\n**Input:**\\n\\n```text\\n$ regression-test\\none parser regression test passed\\n[exit 2]\\n```\\n\\n**Expected:** Reject  \\n\\n**V1c — `[exit 255]` trailer (Reject)**  \\n**Input:**\\n\\n```text\\n$ cmd\\noutput\\n[exit 255]\\n```\\n\\n**Expected:** Reject  \\n\\n**V1d — minimal nonzero (Reject)**  \\n**Input:**\\n\\n```text\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V1e — minimal zero (Accept)**  \\n**Input:**\\n\\n```text\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n### V2 — Literal `exit 1` in echoed command; trailer governs\\n\\n**V2a — echo quoted literal + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ echo \\\"exit 1\\\"\\nexit 1\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V2b — echo quoted literal + nonzero trailer (Reject)**  \\n**Input:**\\n\\n```text\\n$ echo \\\"exit 1\\\"\\nexit 1\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V2c — unquoted echo + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ echo exit 1\\nexit 1\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V2d — false; echo + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ false; echo exit 1\\nexit 1\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V2e — printf-style payload + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ printf '%s\\\\n' 'exit 1'\\nexit 1\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V2f — long sentence containing exit 1 + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ echo \\\"the status was exit 1 in the message\\\"\\nthe status was exit 1 in the message\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n### V3 — Structural precedence over payload\\n\\n**V3a — payload looks like success marker; true trailer nonzero (Reject)**  \\n**Input:**\\n\\n```text\\n$ run\\n[exit 0]\\nstill running\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n(Last structural marker is `[exit 1]`.)\\n\\n**V3b — payload contains `[exit 1]` mid-line; trailer zero (Accept)**  \\n**Input:**\\n\\n```text\\n$ echo [exit 1]\\n[exit 1]\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n(Line `[exit 1]` alone would be structural if it were last; here last line is `[exit 0]`. Note: the middle line *is* a structural marker under R1, but R3 last-wins → Accept.)\\n\\n**V3c — mixed echo literal + final nonzero (Reject)**  \\n**Input:**\\n\\n```text\\n$ echo \\\"run exit 1 now\\\"\\nrun exit 1 now\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V3d — two structural markers, last zero (Accept)**  \\n**Input:**\\n\\n```text\\n$ step\\n[exit 1]\\nretry ok\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n### V4 — Non-marker forms (must not drive nonzero Reject)\\n\\n**V4a — bare `exit 1` only (Malformed)**  \\n**Input:**\\n\\n```text\\n$ echo hi\\nhi\\nexit 1\\n```\\n\\n**Expected:** Malformed  \\n(No R1 structural marker.)\\n\\n**V4b — parenthesized form + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ (exit 1)\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V4c — uppercase variant only (Malformed)**  \\n**Input:**\\n\\n```text\\n$ cmd\\n[EXIT 1]\\n```\\n\\n**Expected:** Malformed  \\n\\n**V4d — leading-zero variant only (Malformed)**  \\n**Input:**\\n\\n```text\\n$ cmd\\n[exit 01]\\n```\\n\\n**Expected:** Malformed  \\n\\n**V4e — internal whitespace variant only (Malformed)**  \\n**Input:**\\n\\n```text\\n$ cmd\\n[exit 1 ]\\n```\\n\\n**Expected:** Malformed  \\n\\n**V4f — mid-line marker-like text + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ echo foo [exit 1] bar\\nfoo [exit 1] bar\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V4g — trailing semicolon junk line + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ cmd\\nexit 1;\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n### V5 — Empty / trailer-only / whitespace body\\n\\n**V5a — empty string (Malformed)**  \\n**Input:** *(empty — zero bytes)*  \\n\\n**Expected:** Malformed  \\n\\n**V5b — whitespace only (Malformed)**  \\n**Input:**\\n\\n```text\\n\\n\\n```\\n\\n**Expected:** Malformed  \\n(Exact bytes: `\\\\n\\\\n` only.)\\n\\n**V5c — body + zero (Accept)**  \\n**Input:**\\n\\n```text\\nsome output\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V5d — body + nonzero (Reject)**  \\n**Input:**\\n\\n```text\\nsome output\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n---\\n\\n### V6 — Absent vs truncated (Malformed; do not invent nonzero)\\n\\n**V6a — no marker after output (Malformed)**  \\n**Input:**\\n\\n```text\\n$ regression-test\\none parser regression test passed\\n```\\n\\n**Expected:** Malformed  \\n\\n**V6b — truncated opener, no close (Malformed)**  \\n**Input:**\\n\\n```text\\n$ cmd\\noutput\\n[exit 1\\n```\\n\\n**Expected:** Malformed  \\n\\n**V6c — truncated empty brackets line (Malformed)**  \\n**Input:**\\n\\n```text\\n$ cmd\\n[exit\\n```\\n\\n**Expected:** Malformed  \\n\\n**V6d — close bracket missing digit (Malformed)**  \\n**Input:**\\n\\n```text\\n$ cmd\\n[exit ]\\n```\\n\\n**Expected:** Malformed  \\n\\n---\\n\\n### V7 — Boundary and adversarial fixtures (exact bytes)\\n\\n**V7a — duplicate final markers last nonzero (Reject)**  \\n**Input:**\\n\\n```text\\n$ cmd\\nok\\n[exit 0]\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V7b — duplicate final markers last zero (Accept)**  \\n**Input:**\\n\\n```text\\n$ cmd\\nbad\\n[exit 1]\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n**V7c — missing final newline after zero marker (Accept)**  \\n**Input (exact; final line has no trailing `\\\\n`):**  \\nbytes = `$ cmd\\\\ndone\\\\n[exit 0]`  \\n\\nFor copy-paste clarity, the three lines are `$ cmd`, `done`, `[exit 0` with **no** newline after the closing `]` of the marker. Host must feed exactly that byte string.\\n\\n**Expected:** Accept  \\n(R1 whole-line marker after LF normalization of prior lines; absence of trailing newline after the marker line does not strip marker status.)\\n\\n**V7d — CRLF line endings with zero marker (Accept)**  \\n**Input (exact CRLF):**  \\nbytes = `$ regression-test\\\\r\\\\none parser regression test passed\\\\r\\\\n[exit 0]\\\\r\\\\n`  \\n\\n**Expected:** Accept  \\n(Host normalizes CRLF→LF before R1, or parser accepts CRLF equivalently; either way outcome must be Accept.)\\n\\n**V7e — CRLF with nonzero marker (Reject)**  \\n**Input (exact CRLF):**  \\nbytes = `$ regression-test\\\\r\\\\none parser regression test passed\\\\r\\\\n[exit 1]\\\\r\\\\n`  \\n\\n**Expected:** Reject  \\n\\n**V7f — embedded NUL before zero trailer (Malformed)**  \\n**Input (exact):**  \\nbytes = `$ cmd\\\\nout\\\\x00put\\\\n[exit 0]\\\\n`  \\n\\n**Expected:** Malformed  \\n(NUL ⇒ boundary/encoding failure per R7; do not Accept merely because a zero trailer appears after binary corruption.)\\n\\n**V7g — nested transcript-looking echo + zero trailer (Accept)**  \\n**Input:**\\n\\n```text\\n$ cat <<'EOF'\\n$ inner\\n[exit 1]\\nEOF\\n$ inner\\n[exit 1]\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n(Last structural marker is `[exit 0]`. Payload may contain full fake transcripts.)\\n\\n**V7h — nested fake transcript + nonzero trailer (Reject)**  \\n**Input:**\\n\\n```text\\n$ cat <<'EOF'\\n$ inner\\n[exit 0]\\nEOF\\n$ inner\\n[exit 0]\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V7i — only prompt line (Malformed)**  \\n**Input:**\\n\\n```text\\n$ \\n```\\n\\n**Expected:** Malformed  \\n\\n**V7j — marker immediately after prompt line (Accept)**  \\n**Input:**\\n\\n```text\\n$ true\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n### V8 — Mutation oracle (derived from V0; exact strings)\\n\\n**V8a — V0 with final marker flipped to 1 (Reject)**  \\n**Input:**\\n\\n```text\\n$ regression-test\\none parser regression test passed\\n[exit 1]\\n```\\n\\n**Expected:** Reject  \\n\\n**V8b — V0 with literal inserted early; marker still 0 (Accept)**  \\n**Input:**\\n\\n```text\\n$ regression-test\\nnote: would have been exit 1\\none parser regression test passed\\n[exit 0]\\n```\\n\\n**Expected:** Accept  \\n\\n---\\n\\n## Host execution procedure (pending)\\n\\nAll steps are **pending** until the active host runs them. No outcomes below are claimed as observed.\\n\\n1. **Normalize harness I/O**  \\n   - Implement or wrap `parse_evidence(transcript: str | bytes) -> Accept | Reject | Malformed` per R1–R9.  \\n   - Record how the real plugin’s API maps onto the three outcomes.\\n\\n2. **Fixed order run**  \\n   Run vectors in this order only:  \\n   `V0, V1a, V1b, V1c, V1d, V1e, V2a, V2b, V2c, V2d, V2e, V2f, V3a, V3b, V3c, V3d, V4a, V4b, V4c, V4d, V4e, V4f, V4g, V5a, V5b, V5c, V5d, V6a, V6b, V6c, V6d, V7a, V7b, V7c, V7d, V7e, V7f, V7g, V7h, V7i, V7j, V8a, V8b`.\\n\\n3. **Per-vector compare**  \\n   For each ID: feed the **exact** input bytes/string; compare actual outcome to the single expected outcome.  \\n   On first mismatch: **abort**, record `FAIL <vector_id> expected=... actual=...`.\\n\\n4. **Idempotence (pending)**  \\n   Re-parse V0, V2a, V1a, V6b once each; second result must equal first.\\n\\n5. **Optional host hardening (not in aggregate pass)**  \\n   - Static dependency inventory of the plugin.  \\n   - Frontier-only default profile smoke log wrapping the same fixture table.  \\n   - Fixture hashes in the evidence pack.  \\n   These must not replace or relax the fixture table.\\n\\n6. **Evidence pack (host returns)**  \\n   - Predicate table: `vector_id → expected → actual → pass/fail`.  \\n   - Full transcripts for any failure.  \\n   - Explicit residual paragraph on malformed boundaries (mandatory; see below).  \\n   - Statement that V0 supplied evidence alone did not prove V1–V8.\\n\\n---\\n\\n## Aggregate gate\\n\\n**PLAN EXECUTION PASS** if and only if:\\n\\n1. Every vector in the frozen table (V0–V8b inclusive) yields its **single** normative expected outcome; and  \\n2. Idempotence checks on V0, V2a, V1a, V6b pass; and  \\n3. The evidence pack includes the mandatory malformed-boundary residual paragraph below.\\n\\n**PLAN EXECUTION FAIL** if any listed vector mismatches, any vector was substituted with a different fixture under the same ID, or the residual paragraph is omitted.\\n\\nOptional dep inventory / profile smoke may be reported separately; they are **not** required for PLAN EXECUTION PASS under this plan’s evidence constraints.\\n\\n---\\n\\n## Minority residual (mandatory — not discharged by green happy-path)\\n\\n**Malformed transcript boundaries** remain a non-blocking residual risk even if every vector above passes:\\n\\n- Truncated or split markers, missing delimiters, fused multi-session transcripts, dual conflicting trailers under alternate grammars, non-UTF8/control bytes, and streaming chunk boundaries can still cause false Accept or false Reject in production paths not identical to these whole-string fixtures.\\n- Policy locked here: **do not invent nonzero exit status from garbage** (truncated `[exit 1` → Malformed, not Reject). Fail closed into **Malformed** for absent/truncated/NUL cases rather than silently repairing boundaries.\\n- Green results on V0–V8b do **not** prove streaming correctness, partial-read assembly, or all encoding shift cases.\\n- Host release notes / evidence pack **must** restate this residual and point at V6a–d and V7c–f as the dedicated negative/boundary slice.\\n\\nSupported emphases retained:\\n\\n- Prefer not false-rejecting by synthesizing nonzero codes from partial markers.  \\n- Never silently repair broken boundaries.  \\n- Future hardening (single canonical trailer window, reject on undecidable fusion, length/checksum sidecar) is out of scope for this pass gate but recommended next iteration.\\n\\n---\\n\\n## Assumptions and remaining uncertainty (explicit)\\n\\n| Topic | Plan choice | Residual uncertainty |\\n|-------|-------------|----------------------|\\n| Marker grammar | Exact whole-line `[exit N]` per R1 | Real plugin may use another dialect (`exit code: N`, bare status). If so, host must remap dialect → R1 equivalent **or** fail plan with grammar mismatch—do not silently broaden fixtures. |\\n| Bare `exit 1` | Non-marker → Malformed if alone | Plugin might treat bare status as structural; that would fail V4a against this plan and requires a documented rule change + fixture revision. |\\n| Multiple markers | Last wins (R3) | Alternate “any nonzero rejects” policy would fail V3d/V7b; lock is intentional. |\\n| Absent marker | Malformed | Not success-by-default. |\\n| Reject API shape | Exception vs structured object | Host documents mapping; plan only requires three-way classification. |\\n| Dependency-free plugin | Harness is dep-free; plugin inventory optional | Cannot be proven from supplied `[exit 0]` transcript alone. |\\n| Command-prefix `$` validation | Out of scope; markers only | Untested whether invalid prompts matter. |\\n| Unicode homoglyphs | Not all homoglyphs enumerated | V4 non-ASCII beyond NUL/CRLF not exhaustively listed; residual risk. |\\n\\n---\\n\\n## Failure handling\\n\\n| Failure | Host action |\\n|---------|-------------|\\n| Any vector mismatch | FAIL plan; do not ship parser as verified for this contract |\\n| Parser lacks Malformed channel | Map errors → Malformed; if absent markers become Accept, FAIL V6a/V4a/V5a |\\n| Grammar differs from R1 | Record mismatch; either adapt documented dialect in a revised plan or reject implementation |\\n| Nondeterministic results on re-parse | FAIL idempotence |\\n| Host cannot feed exact bytes (NUL/CRLF) | FAIL V7d–f with environment note; do not skip quietly |\\n\\n---\\n\\n## Completeness checklist (pre-execution)\\n\\n- [x] Reject actual nonzero structural markers (V1*, V2b, V3a/c, V5d, V7a/e/h, V8a)  \\n- [x] Ignore literal `exit 1` inside echoed commands when trailer is zero (V2a/c/d/e/f, V8b)  \\n- [x] Structural precedence over payload (V3*)  \\n- [x] Supplied evidence baseline Accept (V0) without over-claiming  \\n- [x] Every vector has fully embedded exact input and exactly one expected outcome  \\n- [x] Malformed-boundary minority risk preserved with dedicated fixtures + mandatory residual paragraph  \\n- [x] No seat workspace access or execution claimed beyond supplied evidence  \\n- [x] Aggregate pass defined solely on the frozen fixture table + idempotence + residual paragraph  \\n\\n**Pending:** All actual parse results, smoke logs, and pass/fail marks await active host execution.\",\n  \"minority_findings\": [\n    \"Malformed transcript boundaries are the mandated minority residual across all seats: truncated/partial markers, missing final newline, split/fused transcripts, dual conflicting trailers, and non-UTF8/control bytes can cause false accept or false reject; well-formed `[exit 0]` evidence does not resolve this.\",\n    \"Seat A’s supported lone emphasis: do not false-reject by inventing a nonzero exit from garbage/truncated markers; document boundary behavior explicitly even if the core suite passes.\",\n    \"Seat B’s supported lone emphasis: keep fail-closed as default safety posture, mandate a dedicated boundary negative test and release-note residual, and never silently repair broken boundaries.\",\n    \"Seat C’s supported lone emphasis: future hardening options (single canonical marker in final N lines, reject conflicting trailers, length/checksum sidecar) without blocking the current plan.\"\n  ],\n  \"required_checks\": {\n    \"completion_requires\": [\n      \"requested_change\",\n      \"verification_evidence\",\n      \"post_execution_pass\"\n    ],\n    \"lifecycle_gates\": {\n      \"final\": {\n        \"owner\": \"codex_host\",\n        \"required_evidence\": [\n          \"gate_verdict\",\n          \"cost_ledger\",\n          \"provenance\"\n        ],\n        \"timeout_seconds\": 900,\n        \"tool_policy\": \"none\"\n      },\n      \"plan\": {\n        \"owner\": \"codex_host\",\n        \"required_evidence\": [\n          \"requirements_trace\",\n          \"risk_analysis\"\n        ],\n        \"timeout_seconds\": 900,\n        \"tool_policy\": \"none\"\n      },\n      \"post_execution\": {\n        \"owner\": \"codex_host\",\n        \"required_evidence\": [\n          \"diff\",\n          \"tests\",\n          \"requirement_coverage\"\n        ],\n        \"timeout_seconds\": 1200,\n        \"tool_policy\": \"none\"\n      },\n      \"pre_execution\": {\n        \"owner\": \"codex_host\",\n        \"required_evidence\": [\n          \"approved_fused_plan\",\n          \"scope_boundaries\"\n        ],\n        \"timeout_seconds\": 900,\n        \"tool_policy\": \"none\"\n      },\n      \"summarize\": {\n        \"owner\": \"codex_host\",\n        \"required_evidence\": [\n          \"decisions\",\n          \"open_risks\",\n          \"verification_state\"\n        ],\n        \"timeout_seconds\": 600,\n        \"tool_policy\": \"none\"\n      }\n    },\n    \"require_diff_review\": true,\n    \"run_tests\": true\n  }\n}",
  "lifecycle": {
    "host_receipts_required": true,
    "later_gates": [
      "post_execution",
      "final",
      "summarize"
    ],
    "pending_gates": [
      "plan",
      "pre_execution"
    ],
    "stage_owner": "codex_host"
  },
  "mutation_authorized": false,
  "ready": false,
  "ready_for_host_workflow": true,
  "requires_explicit_confirmation": false,
  "run_id": "codex-0144-frontier-smoke-001",
  "schema_version": 2,
  "selected_profile": "maximum_intelligence",
  "status": "awaiting_host_gates",
  "synthesis_gate": {
    "artifact_sha256": "f16de83c9f1d7ef56e01939fbf17d0b22e6299621df7871021dc57c44ad25219",
    "owner": "mcp_runtime",
    "passed": true
  }
}
